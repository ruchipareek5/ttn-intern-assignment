CREATE PROCEDURE simpleproc (IN user_id int,IN user_name varchar(50),IN gender varchar(50),In age int)
BEGIN
    insert into student (userid,age,username,gender) values (user_id,age,user_name,gender);
END

call procedure ttn.simpleproc;



delimiter //
create trigger trigger1 after update on userdetails
for each row
begin
insert into audit(oldemail,newemail) values (old.email,new.email);
end;//
